一致性哈希学习：
    一、特性
        1. 平衡性
            hash的结果尽可能的分布到所有的缓冲中去
        2. 单调性
            如果已经有一些内容通过hash分配到相应的缓冲中去，此时有新的缓冲区加入，
            hash的结果应该能够保证原有已分配的内容能映射到新的缓冲区去
        3.分散性
            在分布式环境下，终端有可能看不到全部的缓冲，当终端希望通过hash将内容存储到
            缓冲上时，由于不同终端看到的缓冲范围可能有所不同，导致相同的内容被存储到不同的缓冲中去
        4.负载
            负载换了一个角度，既然不同的终端会将相同的内容存储到不同的缓冲中去，那么，假设一个缓冲区
            会被不同的用户映射为不同的内容

    二、一致性
        1. 强一致性：在更新操作完成之后，任何后续的访问看到的都是更新后的数据
        2. 弱一致性：系统不能保证后续访问返回更新之后的值，必须要满足一些条件才能返回更新后的值
        3. 最终一致性：弱一致性的特殊形式，存储系统保证如果没有对某个对象新更新操作，最终所有的访问返回这个对象最后一次更新的值