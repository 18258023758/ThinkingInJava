库学习：
    一、六大范式
        第一范式：对于表中每一行，必须且仅有唯一的行值，在一行中的每一列仅有唯一的值并且原子性
        第二范式：要求非主键列是主键列的子集，非主键活动必须依赖于主键，主键一旦创建，无法改变
        第三范式：非主键列不相互依赖
        BCNF：主键列不相互依赖
        第四范式：禁止多值依赖
        第五范式：将表分割成极可能的几小块
    二、数据库锁
        （一）行级锁（一种排他锁）
            关键字：WAIT
            特点：开锁大，加锁慢，会出现死锁，锁粒度最小，发生锁冲突的概率最低
        （二）表级锁
